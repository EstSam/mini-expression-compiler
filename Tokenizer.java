//Converts input into tokens
//Breaks the input string into tokens such as numbers, operators, and parentheses.

import java.util.Scanner;

public class Tokenizer extends Token{

    public Tokenizer(String value, Token.Tokens type) {
        super(value, type);
    
    }
    Scanner scan = new Scanner(System.in);
    //initialized Scanner
    int input  = scan.nextInt();
    //check if int is within input

    





}